# Dokumentacja Modeli (Django ORM)

Ta sekcja zawiera automatycznie wygenerowaną dokumentację dla klas modeli, bazującą na docstringach w kodzie.

## Modele Aplikacji Quizy (`quizzes.models`)

::: quizzes.models.Quiz
    options:
      members:
        - can_edit
        - can_view

::: quizzes.models.Question
    options:
      members: true

::: quizzes.models.Answer
    options:
      members: true

::: quizzes.models.QuizAttempt
    options:
      members: true

## Modele Aplikacji Użytkownicy (`accounts.models`)

::: accounts.models.User
    options:
      members: true