# Modele Użytkowników

Dokumentacja modeli z aplikacji `accounts`. Głównym elementem jest niestandardowy model użytkownika, który zastępuje domyślny model Django, zapewniając elastyczność w rozwoju aplikacji.

## Użytkownik Systemowy

::: accounts.models.User
    options:
      show_root_heading: true
      members: false